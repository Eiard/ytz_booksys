package controller.controllerEnum;

public enum BorrowEnum {
    /**
     * 借书成功
     */
    LEND_SUCCESS,
    LEND_FORMAT_ERROR,
    LEND_,

    /**
     * 查询成功
     * 没有借一本书
     */
    QUERY_SUCCESS,
    QUERY_NONE_BOOK_ERROR,

    /**
     * 未知错误
     */
    UNKNOWN_ERROR,
}
